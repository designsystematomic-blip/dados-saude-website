import { Meta, StoryObj } from '@storybook/react';
import {{pascalCase part}} from './{{pascalCase part}}';

const meta: Meta<typeof {{pascalCase part}}> = {
    title: 'Components/{{ pascalCase name }}/{{pascalCase part}}',
    component: {{pascalCase part}},
    tags: ['autodocs'],
};

export default meta;

type Story = StoryObj<typeof {{pascalCase part}}>;

export const Default: Story = {
    args: {
        // Add default props here if needed
    },
};